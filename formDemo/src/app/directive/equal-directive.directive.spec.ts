import { EqualDirectiveDirective } from './equal-directive.directive';

describe('EqualDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new EqualDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
