import { MobileDirectiveDirective } from './mobile-directive.directive';

describe('MobileDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MobileDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
